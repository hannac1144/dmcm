const labels={'company-owned':'Company-owned technology','founder-owned':'Founder-owned technology','university-owned':'University-controlled technology','licensed-in':'Technology licensed from a third party','jointly-owned':'Jointly owned technology','ownership-unclear':'Ownership needs clarification','ownership-other':'Other ownership arrangement','in-house':'Keep regulatory responsibility in-house','shared':'Share regulatory responsibility with a partner','partner-led':'Partner leads regulatory responsibility','undetermined':'Regulatory responsibility still to be decided'};
function textOf(el){return(el?.textContent||'').replace(/\s+/g,' ').trim()}
function sectionFor(title){const h=[...document.querySelectorAll('h1,h2,h3')].find(x=>textOf(x).toLowerCase().includes(title.toLowerCase()));return h?(h.closest('section')||h.parentElement):null}
function recommendations(title,max=5){const scope=sectionFor(title);if(!scope)return[];const cards=[...scope.querySelectorAll('article,.card,.option-card,.recommendation-card,.result-card')];let ranked=cards.filter(c=>/recommended|best fit|strong fit|primary|priority/i.test(textOf(c)));if(!ranked.length)ranked=cards;const out=[];for(const c of ranked){const h=c.querySelector('h2,h3,h4');const name=textOf(h);if(name&&!/why this result|decision tree/i.test(name)&&!out.includes(name))out.push(name);if(out.length>=max)break}return out}
function rankedList(items,why){return `<div class="recap-ranked-list">${items.slice(0,5).map((x,i)=>`<div class="recap-ranked-item"><div class="recap-rank">${i+1}</div><div><strong>${x}</strong><p>${i===0?'Best starting point based on the answers provided.':why}</p></div></div>`).join('')}</div>`}
function makeRecap(){if(document.querySelector('[data-personalized-recap]'))return;const summary=[...document.querySelectorAll('h1,h2')].find(h=>textOf(h).includes('You’ve mapped the key decisions'));if(!summary)return;const section=summary.closest('section');if(!section)return;
 const funding=recommendations('How should you fund and develop the program?',5),license=recommendations('What type of license should you consider?',5),terms=recommendations('Which contractual terms deserve your attention?',5);const ownership=sessionStorage.getItem('healthtech-ownership')||'',reg=sessionStorage.getItem('healthtech-regulatory-responsibility')||'';
 const recap=document.createElement('section');recap.dataset.personalizedRecap='true';recap.className='personalized-recap';recap.innerHTML=`<span class="eyebrow">Your personalized recap</span><h2>Your recommended path at a glance</h2><p class="recap-lead">Below are five recommendation summaries. Within each category, the most relevant options are shown in a simple 1–5 order where multiple options apply, so you can quickly see what to explore first and what to keep as alternatives.</p><div class="recap-cards">
 <article class="recap-card"><div class="recap-number">1</div><div><span class="recap-label">Recommendation summary 1</span><h3>Financing & development</h3>${funding.length?rankedList(funding,'A secondary pathway worth considering if the primary option is unavailable or the deal terms are less attractive.'):'<p>Review the financing and development recommendations above for the pathways generated from your answers.</p>'}<p class="recap-next"><strong>Focus:</strong> Match the financing amount, development stage, control preferences and capabilities you need to the funding source or strategic partner.</p></div></article>
 <article class="recap-card"><div class="recap-number">2</div><div><span class="recap-label">Recommendation summary 2</span><h3>Licensing structure</h3>${license.length?rankedList(license,'An alternative structure that may preserve different rights or provide different incentives to a partner.'):'<p>Review the licensing recommendations above for the structures generated from your answers.</p>'}<p class="recap-next"><strong>Focus:</strong> Decide how much exclusivity to give while protecting the indications, territories, products and future uses you want to retain.</p></div></article>
 <article class="recap-card"><div class="recap-number">3</div><div><span class="recap-label">Recommendation summary 3</span><h3>Contract terms to prioritize</h3>${terms.length?rankedList(terms,'Another important provision to address in the term sheet and definitive agreement.'):'<p>Review the contractual-term recommendations above for the priorities generated from your answers.</p>'}<p class="recap-next"><strong>Focus:</strong> Put the highest-priority terms on the negotiation checklist early, especially provisions affecting economics, control, performance, IP and termination.</p></div></article>
 <article class="recap-card"><div class="recap-number">4</div><div><span class="recap-label">Recommendation summary 4</span><h3>Regulatory responsibility</h3><div class="recap-ranked-list"><div class="recap-ranked-item"><div class="recap-rank">1</div><div><strong>${reg?labels[reg]||reg:'Confirm the regulatory-responsibility structure'}</strong><p>${reg?'This is the regulatory allocation selected in the questionnaire.':'This should be resolved before finalizing the development or licensing structure.'}</p></div></div></div><p class="recap-next"><strong>Focus:</strong> Clearly allocate regulatory strategy, filings, safety, quality, data access, compliance costs and transition responsibilities.</p></div></article>
 <article class="recap-card"><div class="recap-number">5</div><div><span class="recap-label">Recommendation summary 5</span><h3>Technology ownership & legal foundation</h3><div class="recap-ranked-list"><div class="recap-ranked-item"><div class="recap-rank">1</div><div><strong>${ownership?labels[ownership]||ownership:'Confirm ownership and chain of title'}</strong><p>${ownership?'Use this ownership position as the starting point for diligence before granting rights.':'Confirm who owns or controls the technology before financing or licensing it.'}</p></div></div></div><p class="recap-next"><strong>Focus:</strong> Verify assignments, existing licenses, third-party rights, institutional obligations and required consents.</p></div></article>
 </div><div class="recap-bottom"><strong>In simple terms</strong><p>${funding[0]?`Start with <b>${funding[0]}</b> for financing/development. `:''}${license[0]?`For licensing, explore <b>${license[0]}</b> first. `:''}${terms.length?`Prioritize <b>${terms.slice(0,3).join(', ')}</b> in the contract. `:''}Confirm ownership and make sure regulatory responsibility is clearly documented before completing the transaction.</p></div>`;
 const grid=section.querySelector('.summary-grid');if(grid)grid.insertAdjacentElement('beforebegin',recap);else summary.insertAdjacentElement('afterend',recap)}
let t;new MutationObserver(()=>{clearTimeout(t);t=setTimeout(makeRecap,100)}).observe(document.documentElement,{subtree:true,childList:true});makeRecap();